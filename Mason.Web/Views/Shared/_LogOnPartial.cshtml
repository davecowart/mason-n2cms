@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log Off", "Destroy", "Session") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "New", "Session") ]
}
